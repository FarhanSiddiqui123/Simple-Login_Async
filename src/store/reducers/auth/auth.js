/*import * as TYPES from "../../types";
const initialState={

}
const reducer = (state = initialState, action) => {
  switch (action.type) {
    case TYPES.LOGIN:
      return {
        ...state,
        
        isLoggedIn: true, 
      }; 
      
      case TYPES.LOGOUT:
      return {
        ...state,
        
      };
    default:
      return state;
  }
};
export default reducer;*/